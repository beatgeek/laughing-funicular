# Application Settings
PORT=8000

# Qdrant Settings (optional - defaults to in-memory)
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Model caching (optional)
# Set to directory to cache HuggingFace models
# TRANSFORMERS_CACHE=/app/.cache/huggingface
